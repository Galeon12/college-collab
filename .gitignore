# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Lockfiles are not tracked. Note this means `npm ci` will not work on a fresh
# clone (it requires a lockfile) — use `npm install`.
package-lock.json

# Internal working directory: notes, scratch scripts. Not part of the build.
local/

# Durable write spool (dev). In production this lives at /home/data.
server/.data/


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
